namespace Teammate.Interface
{
    /// <summary>The three Study 2 interface conditions, one per seven-minute mission block.</summary>
    public enum DisplayCondition
    {
        NoCue = 0,
        NumericCue = 1,
        IconCue = 2,
    }
}
